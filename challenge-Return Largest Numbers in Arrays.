function largestOfFour(arr) 
{
  myArr= [];
    for(i=0;i<arr.length;i++)
      {
        largestNum = Math.max.apply(0,arr[i]);
        myArr.push(largestNum);
      }
 
return myArr;

}    

largestOfFour([[4, 5, 1, 3], [13, 27, 18, 26],[1000, 1001, 857, 1], [32, 35, 37, 39], ]);
